[RecoveryVaultGeneratedCredentialRepository.cs]
# Explicit ToArray calls create independently owned secret buffers that are zeroed
# separately. The nested type keeps an explicit constructor so its validation and
# secret ownership remain visible.
dotnet_diagnostic.IDE0305.severity = none
dotnet_diagnostic.IDE0290.severity = none
